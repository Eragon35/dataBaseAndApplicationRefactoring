# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~


GET     /calculateDebts/:bankId     controllers.ClearingController.calculateDebts(bankId: String)

POST    /handlePayment              controllers.ClearingController.handlePayment()
